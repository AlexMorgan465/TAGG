const AGENT=window.CURRENT_AGENT;let selected=null;const key='EPITHETE_TREATMENTS_'+AGENT;who.textContent=AGENT;agentName.textContent=AGENT;
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const workKey=x=>[x.agent,x.siret,x.raisonSociale].map(v=>String(v??'').trim().toUpperCase()).join('|');
function history(){try{let a=JSON.parse(localStorage.getItem(key)||'[]'),m=new Map();for(const x of a){let k=workKey(x),old=m.get(k);if(!old||new Date(x.completedAt||0)>new Date(old.completedAt||0))m.set(k,x)}let clean=[...m.values()];if(clean.length!==a.length)localStorage.setItem(key,JSON.stringify(clean));return clean}catch{return []}}
function loadAssignments(){let done=new Set(history().map(workKey)),seen=new Set();let a=(window.AFFECTATIONS?.assignments||[]).filter(x=>{let k=workKey(x);if(x.agent!==AGENT||done.has(k)||seen.has(k))return false;seen.add(k);return true});count.textContent=a.length;list.innerHTML=a.length?a.map(x=>`<div class="assignment" onclick="pick('${x.id}',this)"><div class="assignment-top"><b>${esc(x.raisonSociale)}</b><span class="mini-badge">${esc(x.status||'AFFECTEE')}</span></div><div>SIRET : ${esc(x.siret)} · Type : ${esc(x.typeFacture)}</div><div class="muted">${esc(x.dateLimite||'')} ${x.status==='A_CORRIGER'?'· Correction demandée':''}</div></div>`).join(''):'<div class="empty"><b>Tout est à jour</b><span>Aucune affectation active.</span></div>'}
function pick(id,el){selected=(window.AFFECTATIONS.assignments||[]).find(x=>x.id===id);document.querySelectorAll('.assignment').forEach(x=>x.classList.remove('sel'));el.classList.add('sel');info.innerHTML=`<div class="detail-title">${esc(selected.raisonSociale)}</div><div class="detail-grid"><span>SIRET</span><b>${esc(selected.siret)}</b><span>Type</span><b>${esc(selected.typeFacture)}</b><span>Date limite</span><b>${esc(selected.dateLimite||'-')}</b><span>Commentaire</span><b>${esc(selected.commentaire||'-')}</b></div>`}
function finish(){if(!selected)return alert('Sélectionnez une ligne.');let arr=history();let k=workKey(selected);let t={assignmentId:selected.id,agent:AGENT,siret:selected.siret,raisonSociale:selected.raisonSociale,typeFacture:selected.typeFacture,dateLimite:selected.dateLimite,numeroAbonne:numeroAbonne.value,idt:idt.value,mdp:mdp.value,contratBAD:contratBAD.value,csp:csp.value,commentaire:commentaire.value,status:'A_VERIFIER',completedAt:new Date().toISOString()};arr=arr.filter(x=>workKey(x)!==k);arr.push(t);localStorage.setItem(key,JSON.stringify(arr));let safe=AGENT.replace(/[^A-Za-z0-9_]/g,'_'),text=`window.EPITHETE_CONSOLID_${safe} = ${JSON.stringify({agent:AGENT,generatedAt:new Date().toISOString(),treatments:arr},null,2)};`,blob=new Blob([text],{type:'text/javascript'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=AGENT+'.js';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);selected=null;info.innerHTML='<div class="empty"><b>Aucun dossier sélectionné</b><span>Choisissez une ligne à gauche.</span></div>';['numeroAbonne','idt','mdp','contratBAD','csp','commentaire'].forEach(id=>document.getElementById(id).value='');loadAssignments();alert('Traitement prêt. Confirmez le remplacement de '+AGENT+'.js dans data\\consolid.');}
loadAssignments();
